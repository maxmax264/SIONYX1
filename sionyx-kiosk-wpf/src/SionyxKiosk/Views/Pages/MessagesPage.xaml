<Page x:Class="SionyxKiosk.Views.Pages.MessagesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="׳”׳•׳“׳¢׳•׳×" FlowDirection="RightToLeft">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="׳”׳•׳“׳¢׳•׳×" FontSize="24" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,6" />
                <TextBlock Text="הודעות מהמנהל והפיקוח" FontSize="14"
                           Foreground="{StaticResource TextMutedBrush}" />
            </StackPanel>
        </Border>

        <TabControl Grid.Row="1" x:Name="MainTabControl"
                    Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="FontSize" Value="14" />
                    <Setter Property="Padding" Value="16,10" />
                    <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="TabBorder" Padding="16,10" Margin="0,0,8,0"
                                        CornerRadius="10" Background="Transparent" Cursor="Hand">
                                    <TextBlock x:Name="TabText" Text="{TemplateBinding Header}"
                                               FontSize="14" FontWeight="SemiBold"
                                               Foreground="{StaticResource TextMutedBrush}" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="TabBorder" Property="Background"
                                                Value="{StaticResource PrimaryGhostBrush}" />
                                        <Setter TargetName="TabText" Property="Foreground"
                                                Value="{StaticResource PrimaryBrush}" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="TabBorder" Property="Background"
                                                Value="{StaticResource PrimaryGhostBrush}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <!-- כרטיסיה 1: מנהל -->
            <TabItem x:Name="AdminTab" Header="הודעות מהמנהל">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="White" CornerRadius="14"
                            Padding="0" Margin="0,12,0,12">
                        <Grid>
                            <StackPanel x:Name="AdminLoadingPanel" VerticalAlignment="Center"
                                        HorizontalAlignment="Center" Visibility="Visible">
                                <TextBlock Text="׳˜׳•׳¢׳ ׳”׳•׳“׳¢׳•׳×..." FontSize="14"
                                           Foreground="{StaticResource TextMutedBrush}"
                                           HorizontalAlignment="Center" />
                            </StackPanel>

                            <StackPanel x:Name="AdminEmptyPanel" VerticalAlignment="Center"
                                        HorizontalAlignment="Center" Visibility="Collapsed">
                                <Border Width="64" Height="64" CornerRadius="32"
                                        Background="{StaticResource PrimaryGhostBrush}"
                                        HorizontalAlignment="Center" Margin="0,0,0,16">
                                    <TextBlock Text="&#xE8F2;" FontFamily="Segoe MDL2 Assets"
                                               FontSize="26" Foreground="{StaticResource PrimaryBrush}"
                                               HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <TextBlock Text="אין הודעות" FontSize="16" FontWeight="SemiBold"
                                           Foreground="{StaticResource TextPrimaryBrush}"
                                           HorizontalAlignment="Center" />
                                <TextBlock Text="הודעות מהמנהל יופיעו כאן"
                                           FontSize="13" Foreground="{StaticResource TextMutedBrush}"
                                           HorizontalAlignment="Center" Margin="0,6,0,0" />
                            </StackPanel>

                            <ScrollViewer x:Name="AdminScroll" VerticalScrollBarVisibility="Auto"
                                          Padding="16,12" Visibility="Collapsed">
                                <ItemsControl x:Name="AdminMessagesList">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,10">
                                                <!-- הודעה מהמנהל - שמאל, אפור -->
                                                <Border CornerRadius="14" Padding="12,10"
                                                        MaxWidth="280" HorizontalAlignment="Left"
                                                        Background="#F1F5F9" Margin="0,0,80,0">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsUserReply}" Value="False">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <StackPanel>
                                                        <TextBlock Text="{Binding SenderName}"
                                                                   FontSize="11" FontWeight="Bold"
                                                                   Foreground="#6366F1" Margin="0,0,0,3"/>
                                                        <TextBlock Text="{Binding DisplayBody}"
                                                                   TextWrapping="Wrap" FontSize="14"
                                                                   LineHeight="21" Foreground="#1E293B"/>
                                                        <TextBlock Text="{Binding DisplayTime}"
                                                                   FontSize="11" Foreground="#94A3B8"
                                                                   HorizontalAlignment="Left" Margin="0,5,0,0"/>
                                                    </StackPanel>
                                                </Border>

                                                <!-- תגובת המשתמש - ימין, סגול -->
                                                <Border CornerRadius="14" Padding="12,10"
                                                        MaxWidth="280" HorizontalAlignment="Right"
                                                        Margin="80,0,0,0">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsUserReply}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <Border.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                            <GradientStop Color="#6366F1" Offset="0"/>
                                                            <GradientStop Color="#8B5CF6" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Border.Background>
                                                    <StackPanel>
                                                        <TextBlock Text="אתה" FontSize="11" FontWeight="Bold"
                                                                   Foreground="#C7D2FE"
                                                                   HorizontalAlignment="Right" Margin="0,0,0,3"/>
                                                        <TextBlock Text="{Binding DisplayBody}"
                                                                   TextWrapping="Wrap" FontSize="14"
                                                                   LineHeight="21" Foreground="White"
                                                                   TextAlignment="Right"/>
                                                        <StackPanel Orientation="Horizontal"
                                                                    HorizontalAlignment="Left" Margin="0,5,0,0">
                                                            <Button Content="מחק" Margin="0,0,6,0"
                                                                    Padding="8,2" FontSize="11" Cursor="Hand"
                                                                    Background="#EF4444" Foreground="White"
                                                                    BorderThickness="0" Tag="{Binding Id}"
                                                                    Click="DeleteMessage_Click"/>
                                                            <TextBlock Text="{Binding DisplayTime}"
                                                                       FontSize="11" Foreground="#C7D2FE"
                                                                       VerticalAlignment="Center"/>
                                                        </StackPanel>
                                                    </StackPanel>
                                                </Border>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </Border>

                    <!-- קלט שליחה -->
                    <Border Grid.Row="1" Background="White" CornerRadius="14" Padding="16,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" CornerRadius="10" Background="#F7F8FA"
                                    BorderBrush="#E8EAED" BorderThickness="1"
                                    Padding="12,8" Margin="0,0,10,0">
                                <TextBox x:Name="AdminReplyBox" Text="" AcceptsReturn="False"
                                         BorderThickness="0" Background="Transparent"
                                         FontSize="14" VerticalAlignment="Center" />
                            </Border>
                            <Button Grid.Column="1" x:Name="AdminSendBtn"
                                    Click="AdminSendBtn_Click" Width="44" Height="44" Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="SendBg" CornerRadius="22" Width="44" Height="44">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#6366F1" Offset="0" />
                                                    <GradientStop Color="#8B5CF6" Offset="1" />
                                                </LinearGradientBrush>
                                            </Border.Background>
                                            <TextBlock Text="&#xE724;" FontFamily="Segoe MDL2 Assets"
                                                       FontSize="16" Foreground="White"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="SendBg" Property="Opacity" Value="0.85" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- כרטיסיה 2: פיקוח -->
            <TabItem x:Name="SupervisorTab" Header="הודעות מהפיקוח">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="White" CornerRadius="14"
                            Padding="0" Margin="0,12,0,12">
                        <Grid>
                            <StackPanel x:Name="SupervisorLoadingPanel" VerticalAlignment="Center"
                                        HorizontalAlignment="Center" Visibility="Visible">
                                <TextBlock Text="׳˜׳•׳¢׳ ׳”׳•׳“׳¢׳•׳×..." FontSize="14"
                                           Foreground="{StaticResource TextMutedBrush}"
                                           HorizontalAlignment="Center" />
                            </StackPanel>

                            <StackPanel x:Name="SupervisorEmptyPanel" VerticalAlignment="Center"
                                        HorizontalAlignment="Center" Visibility="Collapsed">
                                <Border Width="64" Height="64" CornerRadius="32"
                                        Background="{StaticResource PrimaryGhostBrush}"
                                        HorizontalAlignment="Center" Margin="0,0,0,16">
                                    <TextBlock Text="&#xE8F2;" FontFamily="Segoe MDL2 Assets"
                                               FontSize="26" Foreground="{StaticResource PrimaryBrush}"
                                               HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <TextBlock Text="׳׳™׳ ׳”׳•׳“׳¢׳•׳× ׳׳”׳₪׳™׳§׳•׳—" FontSize="16" FontWeight="SemiBold"
                                           Foreground="{StaticResource TextPrimaryBrush}"
                                           HorizontalAlignment="Center" />
                                <TextBlock Text="׳”׳•׳“׳¢׳•׳× ׳׳”׳₪׳™׳§׳•׳— ׳™׳•׳₪׳™׳¢׳• ׳›׳׳"
                                           FontSize="13" Foreground="{StaticResource TextMutedBrush}"
                                           HorizontalAlignment="Center" Margin="0,6,0,0" />
                            </StackPanel>

                            <ScrollViewer x:Name="SupervisorScroll" VerticalScrollBarVisibility="Auto"
                                          Padding="16,12" Visibility="Collapsed">
                                <ItemsControl x:Name="SupervisorMessagesList">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,10">
                                                <!-- הודעה מהפיקוח - שמאל, ירוק בהיר -->
                                                <Border CornerRadius="14" Padding="12,10"
                                                        MaxWidth="280" HorizontalAlignment="Left"
                                                        Background="#F0FDF4" Margin="0,0,80,0">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsUserReply}" Value="False">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <StackPanel>
                                                        <TextBlock Text="{Binding SenderName}"
                                                                   FontSize="11" FontWeight="Bold"
                                                                   Foreground="#059669" Margin="0,0,0,3"/>
                                                        <TextBlock Text="{Binding DisplayBody}"
                                                                   TextWrapping="Wrap" FontSize="14"
                                                                   LineHeight="21"
                                                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                                                        <TextBlock Text="{Binding DisplayTime}"
                                                                   FontSize="11"
                                                                   Foreground="{StaticResource TextMutedBrush}"
                                                                   HorizontalAlignment="Left" Margin="0,5,0,0"/>
                                                    </StackPanel>
                                                </Border>

                                                <!-- תגובת המשתמש - ימין, ירוק כהה -->
                                                <Border CornerRadius="14" Padding="12,10"
                                                        MaxWidth="280" HorizontalAlignment="Right"
                                                        Margin="80,0,0,0">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsUserReply}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <Border.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                            <GradientStop Color="#059669" Offset="0"/>
                                                            <GradientStop Color="#10B981" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Border.Background>
                                                    <StackPanel>
                                                        <TextBlock Text="אתה" FontSize="11" FontWeight="Bold"
                                                                   Foreground="#A7F3D0"
                                                                   HorizontalAlignment="Right" Margin="0,0,0,3"/>
                                                        <TextBlock Text="{Binding DisplayBody}"
                                                                   TextWrapping="Wrap" FontSize="14"
                                                                   LineHeight="21" Foreground="White"
                                                                   TextAlignment="Right"/>
                                                        <StackPanel Orientation="Horizontal"
                                                                    HorizontalAlignment="Left" Margin="0,5,0,0">
                                                            <Button Content="מחק" Margin="0,0,6,0"
                                                                    Padding="8,2" FontSize="11" Cursor="Hand"
                                                                    Background="#FEE2E2" Foreground="#DC2626"
                                                                    BorderThickness="0" Tag="{Binding Id}"
                                                                    Click="DeleteMessage_Click"/>
                                                            <TextBlock Text="{Binding DisplayTime}"
                                                                       FontSize="11" Foreground="#A7F3D0"
                                                                       VerticalAlignment="Center"/>
                                                        </StackPanel>
                                                    </StackPanel>
                                                </Border>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </Border>

                    <!-- קלט שליחה -->
                    <Border Grid.Row="1" Background="White" CornerRadius="14" Padding="16,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" CornerRadius="10" Background="#F7F8FA"
                                    BorderBrush="#E8EAED" BorderThickness="1"
                                    Padding="12,8" Margin="0,0,10,0">
                                <TextBox x:Name="SupervisorReplyBox" Text="" AcceptsReturn="False"
                                         BorderThickness="0" Background="Transparent"
                                         FontSize="14" VerticalAlignment="Center" />
                            </Border>
                            <Button Grid.Column="1" x:Name="SupervisorSendBtn"
                                    Click="SupervisorSendBtn_Click" Width="44" Height="44" Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="SendBg2" CornerRadius="22" Width="44" Height="44">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#059669" Offset="0" />
                                                    <GradientStop Color="#10B981" Offset="1" />
                                                </LinearGradientBrush>
                                            </Border.Background>
                                            <TextBlock Text="&#xE724;" FontFamily="Segoe MDL2 Assets"
                                                       FontSize="16" Foreground="White"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="SendBg2" Property="Opacity" Value="0.85" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Page>
