<Page x:Class="SionyxKiosk.Views.Pages.PackagesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="clr-namespace:SionyxKiosk.Views.Controls"
      Title="חבילות" FlowDirection="RightToLeft">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0">
        <StackPanel Margin="24,0,24,24">
            <controls:PageHeader Title="חבילות" Subtitle="בחר את החבילה המתאימה לך"
                                 HeaderBrush="{StaticResource PackagesHeaderGradient}" Icon="📦"
                                 Margin="0,0,0,28" />

            <!-- Loading -->
            <controls:LoadingSpinner HorizontalAlignment="Center"
                                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" />

            <!-- Empty state -->
            <controls:EmptyState Message="אין חבילות זמינות"
                                 Visibility="{Binding ShowEmptyState, Converter={StaticResource BoolToVis}}" />

            <!-- Package grid - responsive -->
            <ItemsControl ItemsSource="{Binding Packages}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="3" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border x:Name="CardRoot" Background="White"
                                CornerRadius="18" Margin="12" Padding="0" Cursor="Hand"
                                BorderBrush="#E8ECF4" BorderThickness="1.5"
                                MinHeight="380">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="16" ShadowDepth="3" Direction="270"
                                                  Color="#0F172A" Opacity="0.06" />
                            </Border.Effect>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <!-- Card header with price -->
                                <Border Grid.Row="0" Padding="32,28,32,24"
                                        CornerRadius="18,18,0,0">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#EFF6FF" Offset="0" />
                                            <GradientStop Color="#E0F2FE" Offset="1" />
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <!-- Name + discount badge -->
                                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,14">
                                            <TextBlock Text="{Binding Name}" FontSize="26" FontWeight="Bold"
                                                       Foreground="#1E293B" VerticalAlignment="Center" />
                                            <Border CornerRadius="8" Padding="10,4" Margin="12,0,0,0"
                                                    VerticalAlignment="Center"
                                                    Visibility="{Binding HasDiscount, Converter={StaticResource BoolToVis}}">
                                                <Border.Background>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                        <GradientStop Color="#EF4444" Offset="0" />
                                                        <GradientStop Color="#DC2626" Offset="1" />
                                                    </LinearGradientBrush>
                                                </Border.Background>
                                                <TextBlock Text="{Binding DiscountPercent, StringFormat='הנחה {0:F0}%'}"
                                                           FontSize="13" FontWeight="Bold" Foreground="White" />
                                            </Border>
                                        </StackPanel>

                                        <!-- Price row -->
                                        <StackPanel Grid.Row="1" Orientation="Horizontal"
                                                    VerticalAlignment="Bottom">
                                            <TextBlock FontSize="50" FontWeight="ExtraBold" Margin="0,0,0,-4">
                                                <TextBlock.Foreground>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                        <GradientStop Color="#6366F1" Offset="0" />
                                                        <GradientStop Color="#8B5CF6" Offset="1" />
                                                    </LinearGradientBrush>
                                                </TextBlock.Foreground>
                                                <Run Text="₪" FontSize="28" /><Run Text="{Binding DisplayPrice, StringFormat='{}{0:F0}', Mode=OneWay}" />
                                            </TextBlock>
                                            <TextBlock Text="{Binding Price, StringFormat='₪{0:F0}'}"
                                                       FontSize="20" Foreground="#94A3B8"
                                                       TextDecorations="Strikethrough"
                                                       VerticalAlignment="Center" Margin="14,0,0,4"
                                                       Visibility="{Binding HasDiscount, Converter={StaticResource BoolToVis}}" />
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- Feature list -->
                                <StackPanel Grid.Row="1" Margin="32,24,32,10">
                                    <!-- Time -->
                                    <Border Padding="0,13" BorderBrush="#F1F5F9" BorderThickness="0,0,0,1">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" Width="38" Height="38" CornerRadius="10"
                                                    Background="#EEF2FF" Margin="0,0,16,0">
                                                <TextBlock Text="⏱" FontSize="18"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            </Border>
                                            <TextBlock Grid.Column="1" Text="זמן שימוש"
                                                       FontSize="17" Foreground="#64748B"
                                                       VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="2"
                                                       Text="{Binding Minutes, StringFormat='{}{0} דקות'}"
                                                       FontSize="17" FontWeight="Bold" Foreground="#1E293B"
                                                       VerticalAlignment="Center" />
                                        </Grid>
                                    </Border>

                                    <!-- Prints -->
                                    <Border Padding="0,13" BorderBrush="#F1F5F9" BorderThickness="0,0,0,1">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" Width="38" Height="38" CornerRadius="10"
                                                    Background="#F0FDF4" Margin="0,0,16,0">
                                                <TextBlock Text="🖨" FontSize="18"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            </Border>
                                            <TextBlock Grid.Column="1" Text="תקציב הדפסות"
                                                       FontSize="17" Foreground="#64748B"
                                                       VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="2"
                                                       Text="{Binding Prints, StringFormat='{}{0}₪'}"
                                                       FontSize="17" FontWeight="Bold" Foreground="#1E293B"
                                                       VerticalAlignment="Center" />
                                        </Grid>
                                    </Border>

                                    <!-- Validity -->
                                    <Border Padding="0,13">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" Width="38" Height="38" CornerRadius="10"
                                                    Background="#FFF7ED" Margin="0,0,16,0">
                                                <TextBlock Text="📅" FontSize="18"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            </Border>
                                            <TextBlock Grid.Column="1" Text="תוקף"
                                                       FontSize="17" Foreground="#64748B"
                                                       VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="2"
                                                       Text="{Binding ValidityDisplay}"
                                                       FontSize="17" FontWeight="Bold" Foreground="#1E293B"
                                                       VerticalAlignment="Center" />
                                        </Grid>
                                    </Border>
                                </StackPanel>

                                <!-- Buy button -->
                                <Border Grid.Row="2" Padding="28,10,28,28">
                                    <Button Content="רכוש עכשיו" Style="{StaticResource BtnPrimary}"
                                            Command="{Binding DataContext.SelectPackageCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                            CommandParameter="{Binding}" HorizontalAlignment="Stretch"
                                            Height="54" FontSize="18" />
                                </Border>
                            </Grid>

                            <Border.Triggers>
                                <EventTrigger RoutedEvent="MouseEnter">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ThicknessAnimation Storyboard.TargetName="CardRoot"
                                                                Storyboard.TargetProperty="BorderThickness"
                                                                To="2" Duration="0:0:0.15" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <EventTrigger RoutedEvent="MouseLeave">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ThicknessAnimation Storyboard.TargetName="CardRoot"
                                                                Storyboard.TargetProperty="BorderThickness"
                                                                To="1.5" Duration="0:0:0.15" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Border.Triggers>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</Page>
