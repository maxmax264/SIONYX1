<Window x:Class="SionyxKiosk.Views.Dialogs.AdminExitDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="יציאה מהאפליקציה"
        WindowState="Maximized" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" FlowDirection="RightToLeft"
        ShowInTaskbar="False">

    <Grid>
        <!-- Dimmed backdrop -->
        <Border Background="#000000" Opacity="0.45" MouseLeftButtonDown="Cancel_Click" />

        <!-- Dialog card -->
        <Border Width="480" HorizontalAlignment="Center" VerticalAlignment="Center"
                CornerRadius="22" ClipToBounds="True" Padding="40,36"
                Background="White">
            <Border.Effect>
                <DropShadowEffect BlurRadius="40" ShadowDepth="8" Direction="270" Color="#0F172A" Opacity="0.15" />
            </Border.Effect>

            <StackPanel>
                <!-- Warning icon -->
                <Border Width="60" Height="60" CornerRadius="30"
                        Background="#FEF2F2" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <TextBlock Text="⚠" FontSize="28"
                               HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>

                <!-- Title -->
                <TextBlock Text="יציאה מהאפליקציה" FontSize="22" FontWeight="Bold"
                           HorizontalAlignment="Center" Margin="0,0,0,8"
                           Foreground="{StaticResource TextPrimaryBrush}" />

                <!-- Description -->
                <TextBlock Text="הזן סיסמת מנהל כדי לצאת מהאפליקציה. היציאה תסגור את המערכת."
                           FontSize="15" TextWrapping="Wrap" TextAlignment="Center"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,0,0,24" LineHeight="22" />

                <!-- Password label -->
                <TextBlock Text="סיסמת מנהל" FontSize="14" FontWeight="SemiBold"
                           Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8" />

                <!-- Password input -->
                <Border CornerRadius="12" Background="#F8FAFC" BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="2" Padding="16,14" Margin="0,0,0,28">
                    <PasswordBox x:Name="PasswordInput" AutomationProperties.AutomationId="AdminPasswordInput" FontSize="16"
                                 Foreground="{StaticResource TextPrimaryBrush}"
                                 CaretBrush="{StaticResource PrimaryBrush}"
                                 Background="Transparent" BorderThickness="0" />
                </Border>

                <!-- Actions -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" AutomationProperties.AutomationId="AdminExitCancelButton" Content="ביטול" IsCancel="True"
                            Padding="0,14" FontSize="15" FontWeight="SemiBold"
                            Click="Cancel_Click" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bg" Background="#F1F5F9"
                                        CornerRadius="12" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                      TextBlock.Foreground="{StaticResource TextPrimaryBrush}" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bg" Property="Background" Value="#E2E8F0" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button Grid.Column="2" AutomationProperties.AutomationId="AdminExitConfirmButton" Content="אשר יציאה" IsDefault="True"
                            Padding="0,14" FontSize="15" FontWeight="SemiBold"
                            Click="OK_Click" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bg" CornerRadius="12" Padding="{TemplateBinding Padding}">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#EF4444" Offset="0" />
                                            <GradientStop Color="#DC2626" Offset="1" />
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                      TextBlock.Foreground="White" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bg" Property="Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#DC2626" Offset="0" />
                                                    <GradientStop Color="#B91C1C" Offset="1" />
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <!-- Hint -->
                <TextBlock Text="סיסמה נדרשת ליציאה מהמערכת" FontSize="13"
                           Foreground="{StaticResource TextMutedBrush}"
                           HorizontalAlignment="Center" Margin="0,16,0,0" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
