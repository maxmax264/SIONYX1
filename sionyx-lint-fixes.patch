diff --git a/functions/index.js b/functions/index.js
index d80c669..c54c704 100644
--- a/functions/index.js
+++ b/functions/index.js
@@ -4,7 +4,8 @@
  * const {onCall} = require("firebase-functions/v2/https");
  * const {onDocumentWritten} = require("firebase-functions/v2/firestore");
  *
- * See a full list of supported triggers at https://firebase.google.com/docs/functions
+ * See a full list of supported triggers at
+ * https://firebase.google.com/docs/functions
  */
 
 const {setGlobalOptions} = require("firebase-functions");
@@ -208,10 +209,13 @@ exports.nedarimCallback = onRequest(async (req, res) => {
   };
   log.debug("Request payload (masked)", {payload: maskedBody});
 
-  // DIAGNOSTIC: log just the field names Nedarim sent us. Useful for confirming whether a
-  // saved-card token (KevaId / Token / similar) is ever present on this server-side callback,
-  // as opposed to only being returned client-side via the iframe postMessage.
-  log.info("Nedarim callback field names received", {fields: Object.keys(req.body || {})});
+  // DIAGNOSTIC: log just the field names Nedarim sent us. Useful for
+  // confirming whether a saved-card token (KevaId / Token / similar) is
+  // ever present on this server-side callback, as opposed to only being
+  // returned client-side via the iframe postMessage.
+  log.info("Nedarim callback field names received", {
+    fields: Object.keys(req.body || {}),
+  });
 
   try {
     // CALLBACK_SECRET: validate secret from query param or header
@@ -771,7 +775,8 @@ exports.chargeWithSavedCard = onCall(async (request) => {
     // Verify the saved card belongs to the SAME user making the request -
     // never trust a kevaId passed from the client without cross-checking it
     // against what's actually stored for this user.
-    const userRef = admin.database().ref(`organizations/${orgId}/users/${callerUid}`);
+    const userRef = admin.database()
+        .ref(`organizations/${orgId}/users/${callerUid}`);
     const userSnapshot = await userRef.once("value");
     const user = userSnapshot.val();
 
@@ -846,7 +851,8 @@ exports.chargeWithSavedCard = onCall(async (request) => {
     ];
     const isStructuralFailure = (message) => {
       const m = (message || "").toLowerCase();
-      return structuralFailureHints.some((hint) => m.includes(hint.toLowerCase()));
+      return structuralFailureHints.some(
+          (hint) => m.includes(hint.toLowerCase()));
     };
 
     const strategies = [
@@ -867,7 +873,7 @@ exports.chargeWithSavedCard = onCall(async (request) => {
       },
       {
         option: 2,
-        label: "Mosad/ApiValid/KevaId/Tashlumim (matches iframe FinishTransaction2 naming)",
+        label: "Mosad/ApiValid/KevaId/Tashlumim (matches iframe naming)",
         params: {
           Action: "TashlumBodedNew",
           Mosad: mosadId,
@@ -903,12 +909,14 @@ exports.chargeWithSavedCard = onCall(async (request) => {
     const attemptsLog = [];
 
     for (const strategy of strategies) {
-      log.info(`[SAVED-CARD OPTION ${strategy.option}] Attempting Nedarim TashlumBodedNew`, {
+      const tag = `[SAVED-CARD OPTION ${strategy.option}]`;
+      log.info(`${tag} Attempting Nedarim TashlumBodedNew`, {
         orgId, purchaseId, kevaId, amount,
         option: strategy.option, label: strategy.label,
       });
 
-      const chargeTimer = createTimer(`nedarim-tashlum-boded-new-option-${strategy.option}`);
+      const timerName = `nedarim-tashlum-boded-new-option-${strategy.option}`;
+      const chargeTimer = createTimer(timerName);
       let responseText;
       try {
         const nedarimResponse = await fetch(
@@ -918,7 +926,7 @@ exports.chargeWithSavedCard = onCall(async (request) => {
         responseText = await nedarimResponse.text();
       } catch (fetchErr) {
         chargeTimer.end(log.info);
-        log.error(`[SAVED-CARD OPTION ${strategy.option}] Network error calling Nedarim`, fetchErr, {
+        log.error(`${tag} Network error calling Nedarim`, fetchErr, {
           orgId, purchaseId, option: strategy.option,
         });
         attemptsLog.push({option: strategy.option, error: "network-error"});
@@ -930,7 +938,7 @@ exports.chargeWithSavedCard = onCall(async (request) => {
       try {
         attemptParsed = JSON.parse(responseText);
       } catch (parseErr) {
-        log.error(`[SAVED-CARD OPTION ${strategy.option}] Failed to parse Nedarim response`, parseErr, {
+        log.error(`${tag} Failed to parse Nedarim response`, parseErr, {
           orgId, purchaseId, option: strategy.option,
           rawResponseLength: responseText.length,
           rawResponseSample: responseText.slice(0, 200),
@@ -939,7 +947,7 @@ exports.chargeWithSavedCard = onCall(async (request) => {
         continue;
       }
 
-      log.info(`[SAVED-CARD OPTION ${strategy.option}] Nedarim response received`, {
+      log.info(`${tag} Nedarim response received`, {
         orgId, purchaseId, option: strategy.option,
         status: attemptParsed.Status, message: attemptParsed.Message,
       });
@@ -950,7 +958,7 @@ exports.chargeWithSavedCard = onCall(async (request) => {
       });
 
       if (attemptParsed.Status === "OK") {
-        log.info(`[SAVED-CARD OPTION ${strategy.option}] SUCCEEDED - this is the working option`, {
+        log.info(`${tag} SUCCEEDED - this is the working option`, {
           orgId, purchaseId, option: strategy.option,
         });
         parsed = attemptParsed;
@@ -959,16 +967,18 @@ exports.chargeWithSavedCard = onCall(async (request) => {
       }
 
       if (isStructuralFailure(attemptParsed.Message)) {
-        log.warn(`[SAVED-CARD OPTION ${strategy.option}] Structural failure, trying next option`, {
-          orgId, purchaseId, option: strategy.option, message: attemptParsed.Message,
+        log.warn(`${tag} Structural failure, trying next option`, {
+          orgId, purchaseId, option: strategy.option,
+          message: attemptParsed.Message,
         });
         continue;
       }
 
-      // Real decline (insufficient funds, blocked card, etc.) - stop here,
-      // do not try further options against the same card/token.
-      log.warn(`[SAVED-CARD OPTION ${strategy.option}] Real decline - stopping (not trying further options)`, {
-        orgId, purchaseId, option: strategy.option, message: attemptParsed.Message,
+      // Real decline (insufficient funds, blocked card, etc.) - stop
+      // here, do not try further options against the same card/token.
+      log.warn(`${tag} Real decline - stopping, not trying more options`, {
+        orgId, purchaseId, option: strategy.option,
+        message: attemptParsed.Message,
       });
       parsed = attemptParsed;
       optionUsed = strategy.option;
